<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.StockExchange" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!--         <item name="colorPrimary">@color/white</item>-->
        <item name="android:queryHint">@style/SearchHintTextStyle</item>
    </style>
    <style name="SearchHintTextStyle" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColorHint">@color/white</item>
    </style>
</resources>